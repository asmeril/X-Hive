{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ],
  "allow": {
    "http": {
      "scope": [
        "http://localhost:8765/*",
        "http://127.0.0.1:8765/*"
      ]
    }
  }
}
